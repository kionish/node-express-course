diameter = 14
radius = diameter/2;

function aboutTheCircle () {
    console.log(`The circumference of a circle with a diameter of ${diameter} is ${2*Math.PI*radius} and its area is ${Math.PI*radius**2}`);
}
aboutTheCircle();